const config = {};

config.serverPort = 8080;

config.dbConfig = {
    user: "root",
    password: "root",
    host: "localhost",
    port: 8889,
    database: "node-crud-app"
}

module.exports = config;